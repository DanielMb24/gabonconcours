{
  "dependencies": {
    "@radix-ui/react-dialog": "^1.1.15",
    "date-fns": "^4.1.0",
    "lucide-react": "^0.552.0",
    "recharts": "^3.3.0",
    "xlsx": "^0.18.5"
  },
  "name": "gabonconcours-v5",
  "version": "5.0.0",
  "private": true,
  "scripts": {
     "build:full": "cd backend && npm install && cd ../frontend && npm install && npm run build",
    "start": "node backend/server.js"
  },
  "engines": {
    "node": ">=20"
  }
}
